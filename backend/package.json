{
  "name": "ailocks-backend",
  "version": "1.0.0",
  "description": "Ailocks AI2AI Network Backend",
  "main": "dist/server.js",
  "scripts": {
    "dev": "dotenv -e ../.env -- prisma generate && dotenv -e ../.env -- tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "generate": "dotenv -e ../.env -- prisma generate",
    "migrate": "dotenv -e ../.env -- prisma migrate dev",
    "db:push": "dotenv -e ../.env -- prisma generate && dotenv -e ../.env -- prisma db push",
    "studio": "dotenv -e ../.env -- prisma studio",
    "postinstall": "dotenv -e ../.env -- prisma generate"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.4.4",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.10.5",
    "prisma": "latest",
    "tsx": "^4.6.2",
    "typescript": "^5.3.3"
  }
}
